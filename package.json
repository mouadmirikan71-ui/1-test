{
  "name": "backey",
  "version": "1.0.0",
  "private": true,
  "description": "Backey — 2BAC study app with a real Gemini-powered AI tutor (secure backend proxy, streaming, retry/backoff, model fallback).",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
